{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "slope-reporter",
        "hooks": [
          {
            "type": "command",
            "command": "echo '\\n--- Slope Check Complete ---' && find slope-reports -name 'slope-report.md' | sort | tail -1 | xargs head -20 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
